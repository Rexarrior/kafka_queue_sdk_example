# Production installs resolve these exact releases from the package index.
# The Docker example installs the sibling source checkout and verifies the same versions.
kafka_server_sdk==1.0.0
kafka_queue_client==1.0.0
