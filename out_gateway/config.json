{
    "pg_config": {
        "user": "postgres",
        "password_env": "POSTGRES_PASSWORD",
        "host": "postgres_db",
        "port": 5432,
        "database": "postgres"
    },
    "host": "0.0.0.0",
    "port": 7092,
    "last_steps": ["file_storage"],
    "auth": {
        "enabled": true,
        "method": "basic",
        "credentials": [
            {
                "username": "admin",
        "password_env": "KAFKA_QUEUE_AUTH_PASSWORD"
            }
        ],
        "exclude_paths": [
            "/out/healthcheck"
        ]
    }
}
